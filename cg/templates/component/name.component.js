'use strict';

angular.module('{{ name:camelCase }}')
    .component('{{ name:camelCase }}', {
        templateUrl: 'components/{{ name }}/{{ name }}.template.html',
        controller: [function ($scope) {

        }]
    });